spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:postgresql://localhost:5432/ewm_main
    username: postgres
    password: postgres

stats-server:
  url: http://localhost:9090

logging:
  level:
    root: INFO
    ru.yandex.practicum: DEBUG